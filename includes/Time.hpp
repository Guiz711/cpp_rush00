#ifndef TIME_HPP
# define TIME_HPP

#include <string>
#include <time.h>

class Time{
  private:
    time_t    _t = time(NULL);
    int       _deltaTime;
    int       _frameTime;

  public:
    int getDeltaTime( voi)
};

#endif
